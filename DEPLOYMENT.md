# 啤酒仓库管理小程序 · 全流程上线操作指南

> 覆盖从零到上线的完整流程：账号资质 → 导入配置 → 部署后端 → 数据库 → 订阅消息 → 权限初始化 → 功能验收 → 发布上线。
> 当前关键配置（已固化在代码中）：AppID `wxa7be288146b7f6a4`、云环境 ID `cloud1-d8gfkvozw70c4f033`、云函数 `quickstartFunctions`。

---

## 0. 全局速览

| 项目 | 值 |
|------|-----|
| AppID | `wxa7be288146b7f6a4` |
| 云环境 ID | `cloud1-d8gfkvozw70c4f033` |
| 云函数 | `quickstartFunctions`（单函数，`switch(event.type)` 分发） |
| 定时触发器 | `dailyExpiryReminder`，cron `0 0 9 * * * *`（每天 9:00） |
| 数据库集合 | 9 个（categories/beers/batches/transactions/orders/admins/invites/subscriptions/settings） |
| 订阅消息 | 3 个模板（临期/过期/新订单） |
| 基础库 | ≥ 2.2.3（项目配置 2.20.1） |

---

## 1. 一次性准备（账号 / 资质 / 环境）

### 1.1 小程序账号与 AppID
1. 到 [微信公众平台](https://mp.weixin.qq.com) 注册小程序（个人或企业主体）。
2. 登录「微信公众平台 → 开发 → 开发设置」获取 **AppID**（本项目已用 `wxa7be288146b7f6a4`）。
3. 注意：**订阅消息**等能力与主体类型、类目相关；若用于真实商业经营，建议企业主体。

### 1.2 开通云开发 + 创建环境
1. 打开「微信开发者工具」，用上面的 AppID 登录。
2. 顶部工具栏点「云开发」→ 开通（首次会引导创建环境）。
3. 创建环境，记录**环境 ID**（本项目为 `cloud1-d8gfkvozw70c4f033`）。
4. 云开发控制台里能看到「数据库」「存储」「云函数」「静态托管」四个 Tab 即成功。

---

## 2. 导入项目并配置

1. 微信开发者工具 →「导入项目」→ 选择仓库根目录（`project.config.json` 所在目录）。
2. 确认 AppID 正确。
3. 确认 `miniprogramRoot = miniprogram/`、`cloudfunctionRoot = cloudfunctions/`（已在 [project.config.json](project.config.json) 配好）。
4. 确认环境 ID：打开 [miniprogram/app.js](miniprogram/app.js)，`globalData.env` 已填 `cloud1-d8gfkvozw70c4f033`。**若换了环境，需同步改这里**。
5. 基础库版本 ≥ 2.2.3（右上角「详情 → 本地设置 → 调试基础库」）。

---

## 3. 部署后端（云函数 + 触发器）

### 3.1 上传云函数
1. 在左侧文件树找到 `cloudfunctions/quickstartFunctions`。
2. 右键 →「**上传并部署：云端安装依赖**」（无第三方依赖，纯改代码时也可选「上传并部署：所有文件」）。
3. 等控制台输出部署成功。

### 3.2 上传触发器
1. 改过 [config.json](cloudfunctions/quickstartFunctions/config.json) 后（触发器/权限），右键该云函数目录 →「**上传触发器**」。
2. 确认触发器 `dailyExpiryReminder`（每天 9:00）和权限 `subscribeMessage.send` 已生效。

---

## 4. 数据库（集合 + 索引）

### 4.1 集合（自动创建）
- 首次进入小程序，`app.js` 会调用 `initDb` 自动创建 **9 个集合**并写入「默认分类」。
- 集合清单：`categories`、`beers`、`batches`、`transactions`、`orders`、`admins`、`invites`、`subscriptions`、`settings`。
- 若没自动建（老用户本地已标记过初始化），到「云开发控制台 → 数据库」手动建这 9 个集合即可。

### 4.2 索引（手动创建）
路径：云开发控制台 → 数据库 → 对应集合 → **索引管理** → **新建索引**。

| 集合 | 索引字段 | 类型 | 用途 | 是否必须手动 |
|------|---------|------|------|-------------|
| `orders` | `status` + `createdAt` | 组合 | 订单按状态筛选 + 时间排序 | ✅ 必须 |
| `batches` | `beerId` + `expiryDate` | 组合 | 批次查询 | ✅ 建议 |
| `beers` | `categoryId` | 单字段 | 品类按分类筛选 | 一般自动，可顺手建 |
| `orders` | `customerOpenid` | 单字段 | 客人查自己订单 | 一般自动，可顺手建 |

> 组合索引字段顺序：等值过滤在前、排序/范围字段在后（如 `status` 在前、`createdAt` 在后）。
> 若某查询报「索引不存在 / 排序字段不在索引中」，按报错把对应的 `字段 + createdAt` 组合索引补上即可。

---

## 5. 订阅消息模板

### 5.1 申请模板
1. 微信公众平台 → 功能 → **订阅消息** → 在公共模板库选用（或用已申请模板）。
2. 本项目 3 个模板 ID 已填在代码两处（云函数 [index.js](cloudfunctions/quickstartFunctions/index.js) `TEMPLATE_IDS` + 前端 [utils/remind.js](miniprogram/utils/remind.js)），需确认与公众平台**已审核通过**的模板一致：

| 用途 | 模板 ID | 字段 key（必须与模板严格一致，否则发 47003） |
|------|---------|------|
| 临期提醒 | `bhjHh5C2MMwfJma8w1sfjfoXNs67UeiNiyY3xksCSN8` | 到期时间 `time2`、剩余天数 `number3`、品类名 `thing1` |
| 过期提醒 | `bHJ4t-Z5B46yJBlyYXVsSLwBrSAnt90ohhzD_l2N400` | 品类名 `thing1`、备注 `thing5` |
| 新订单提醒 | `z7lJAu3BImoV5ZMMqEzF3YzggFFmkSSn67n175y1a4c` | 订单号 `character_string1`、备注 `thing3`、金额 `amount2` |

3. 若换了模板，需**两处同步改**模板 ID 和字段 key。

### 5.2 权限配置
- [config.json](cloudfunctions/quickstartFunctions/config.json) 已声明 `subscribeMessage.send` 权限；改后记得「上传触发器」。

---

## 6. 权限与角色初始化

- **首位打开者自动成为 owner**（`admins` 集合为空时，`getMyRole` 判定）。
- 后续用「我的 → 管理后台 → 成员管理」：
  - **生成邀请码**：owner 生成 6 位码，客人/其他人在「我的 → 加入管理」输入加入，成为 operator。
  - **录入 openid**：owner 直接录入某微信的 openid 添加为 operator。
  - **移交 owner**：把 operator 提升为 owner（支持多 owner）。
  - **移除成员**：软删除（不能移除自己、不能移除唯一 owner）。

---

## 7. 全流程功能验收（测试清单）

> 建议用两个微信号：A = 管理员（owner），B = 客人（customer）。

### 7.1 初始化 & 角色
- [ ] A 首次打开，进入「我的」显示为管理员/owner。
- [ ] 云开发控制台确认 9 个集合已建、「默认分类」存在。

### 7.2 基础资料
- [ ] A：分类管理新增/重命名/停用。
- [ ] A：仓库管理新增品类（名称/分类/规格/售价/成本/保质期/emoji 图标/实拍图上传）。
- [ ] 商城页看到该品类（含实拍图或 emoji）。

### 7.3 进销存闭环
- [ ] A：进货入库（填生产日期）→ 库存增加、流水有 `in`。
- [ ] A：销售出库（批次组合 + FEFO，先到期先出）→ 库存扣减、流水有 `out`（含金额/利润）。
- [ ] A：库存统计能看到总库存 + 分类汇总 + 临期/过期/未填标记。
- [ ] A：销售统计能看到销售额/净利润/销量/订单数 + 趋势图 + 品类排行。

### 7.4 库存调整
- [ ] A：退货入库（负金额流水）、报废出库（损失流水）、库存盘点（盘盈/盘亏 `adjust` 流水）。
- [ ] A：流水页能按类型/品类/时间筛选并汇总。

### 7.5 消费者商城
- [ ] B：商城页看不到库存、看不到成本价。
- [ ] B：点「＋」加购出现已加数量；购物车增删、去结算。
- [ ] B：下单弹窗填姓名/电话/备注 → 结算 → 下单成功 → 订单进入 `orders`。
- [ ] B：「我的 → 我的订单」能看到自己订单。
- [ ] A：订单管理看到全部订单，可「完成/取消」（取消回补库存）。

### 7.6 多管理员协作
- [ ] A：成员管理生成邀请码 → B「加入管理」→ B 变 operator。
- [ ] B：operator 能进出货/统计，但**看不到成员管理**。
- [ ] A：移交 owner / 移除成员，权限即时生效。

### 7.7 数据导出
- [ ] A：数据导出四对象（流水/库存/销售/盘点）生成 CSV → 复制到 Excel 无乱码。

### 7.8 订阅消息 & 定时提醒
- [ ] A：设置临期阈值（如 45 天）。
- [ ] A：「我的 → 消息提醒订阅」授权三个模板 → 剩余额度增加。
- [ ] 造一批临期/过期批次，等定时触发器（9:00）或手动触发验证推送。
- [ ] B：新下单后 A 收到新订单提醒（如 A 已订阅）。

### 7.9 首页兜底提醒
- [ ] A：首页「待处理提醒」红点 + 临期/过期列表，点击跳库存统计。

---

## 8. 发布上线

1. 微信开发者工具 → 右上角「**上传**」→ 填版本号与备注 → 上传代码。
2. 登录公众平台 →「管理 → 版本管理」→ 开发版本里找到刚上传的包 → 提交审核。
3. 审核通过后 → 发布上线。
4. 上线后注意：
   - 定时触发器依赖云函数已部署，首次上线确认触发器已上传。
   - 订阅消息额度是一次性订阅，管理员需定期重新授权（「我的」页有剩余额度展示与续订入口）。
   - 数据库索引缺失会在查询时报错，按 4.2 补建。

---

## 9. 常见问题（FAQ）

| 现象 | 原因 / 处理 |
|------|-------------|
| 首页提示「云开发环境未配置」 | `app.js` 的 `env` 未填或填错 |
| 云函数调用报「无权限」 | 当前 openid 不在 `admins` 白名单；用 owner 在成员管理里添加 |
| 订阅消息发送报 47003 | 模板字段 key/类型与模板不一致，核对第 5 节映射 |
| 查询报「索引不存在 / 排序字段不在索引」 | 到数据库控制台补建对应组合索引 |
| 定时提醒没触发 | 确认「上传触发器」已执行、cron 时间正确 |
| 商品图不显示 | 图片 cloud fileID 需在 `beers.image` 字段，且存储权限为「所有用户可读」 |
