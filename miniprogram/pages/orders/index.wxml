<!--pages/orders/index.wxml-->
<view class="page">
  <view class="filter-row">
    <picker mode="selector" range="{{statusOptions}}" range-key="label" value="{{statusIndex}}" bindchange="onStatusChange">
      <view class="filter-picker">{{statusOptions[statusIndex].label}}</view>
    </picker>
  </view>

  <view wx:if="{{!list.length}}" class="empty">暂无订单</view>

  <view class="card order-card" wx:for="{{list}}" wx:key="_id">
    <view class="order-top">
      <view class="order-no">{{item.orderNo}}</view>
      <view class="tag {{item.statusCls}}">{{item.statusLabel}}</view>
    </view>
    <view class="order-time">{{item.timeText}}</view>
    <view class="order-customer">👤 {{item.customerName}} · {{item.customerPhone}}</view>
    <view class="order-items">
      <view class="order-item" wx:for="{{item.items}}" wx:for-item="it" wx:key="beerId">
        <text class="oi-name">{{it.name}}</text>
        <text class="oi-qty">×{{it.quantity}}</text>
      </view>
    </view>
    <view wx:if="{{item.remark}}" class="order-remark">备注：{{item.remark}}</view>
    <view class="order-bottom">
      <text class="order-count">共 {{item.totalQuantity}} 瓶</text>
      <text class="order-amount">¥{{item.amountText}}</text>
    </view>
    <view wx:if="{{item.status === 'pending'}}" class="order-actions">
      <button class="btn-plain action-btn" data-id="{{item._id}}" bindtap="onCancel">取消</button>
      <button class="btn-primary action-btn" data-id="{{item._id}}" bindtap="onComplete">完成</button>
    </view>
  </view>
</view>
