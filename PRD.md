# 啤酒仓库管理小程序 · 产品需求文档（PRD）

> 版本：v1.5
> 日期：2026-08-13
> 状态：初稿（待评审）
> 技术底座：微信小程序 + 腾讯云开发 CloudBase（服务端云函数 + 文档数据库 + 云存储）

---

## 1. 项目概述

### 1.1 背景与目标

现有仓库管理依赖人工台账或纸质记录，进货、销售、库存、价格与保质期等信息分散，容易出现：

- 库存数量不准，进销存对不上账；
- 啤酒临期/过期无提醒，造成报废损失；
- 售价、成本、保质期等信息缺乏统一维护入口；
- 销售额、净利润无按日/月/年的自动统计，经营状况不透明。

本项目旨在做一个**啤酒仓库管理 + 消费者商城**一体化的微信小程序：管理人员实现「进销存 + 价格维护 + 保质期预警 + 经营统计 + 订单管理」的一站式管理，普通客人则浏览啤酒种类与售价、加购下单；界面**简洁大方、易于操作**（客人端带啤酒视觉元素），并支持**多个管理员协作**与**自定义分类**。

### 1.2 核心价值

| 场景 | 价值 |
|------|------|
| 进货入库 | 一键增加库存，自动生成入库流水 |
| 销售出库 | 按批次组合出库，自动生成销售记录 |
| 库存统计 | 实时查看仓库啤酒总量与各品类数量 |
| 仓库管理 | 集中维护售价、成本、保质期，查看各品类剩余库存 |
| 过期提醒 | 临期/过期自动通过微信订阅消息提醒 |
| 流水与经营分析 | 流水明细 + 销售额/净利润 + 趋势图，经营状况一目了然 |
| 退货/报废/盘点 | 覆盖出入库之外的库存调整场景，账实相符 |
| 多管理员协作 | 多人共同出入库与查看，操作留痕 |
| 自定义分类 | 按自己的经营习惯定义啤酒分类 |
| 消费者商城 | 客人浏览在售啤酒、加购下单、生成订单（线下交付） |

### 1.3 范围

- **In scope**：啤酒品类（SKU）管理、自定义分类、进货入库、销售出库（批次组合）、退货入库、报废出库、库存盘点（支持局部）、库存统计、价格/成本/保质期维护、临期与过期订阅消息提醒、出入库流水、销售额与净利润统计、趋势图、数据导出（Excel/CSV）、多管理员与权限、消费者商城（商品展示/购物车/下单/订单管理）。
- **Out of scope（本期不做）**：多仓库/多门店、供应商管理、会员/客户管理、财务对账、条码扫描、打印小票。

---

## 2. 目标用户与使用角色

| 角色 | 说明 | 主要使用功能 |
|------|------|--------------|
| 所有者（owner） | 仓库所有者，首位使用者 | 全部功能 + 成员管理（独有） |
| 操作员（operator） | 被邀请加入的管理员 | 除「成员管理」外全部业务功能：出入库、退货/报废/盘点、新增/修改/删除品类与分类（含售价/成本/保质期）、查看数据与统计、导出、订单管理 |
| 消费者（customer） | 普通微信用户（非白名单） | 仅浏览在售啤酒种类与售价、加购、下单、查看自己的订单 |

> **多管理员模式**：同一仓库的数据由所有授权管理员共享，每位管理员都可进行出入库操作与查看数据；操作写入 `operatorId` 留痕，便于追溯「谁在何时做了什么」。权限细则见 §3.13。

---

## 3. 核心功能需求

### 3.1 啤酒品类管理（SKU 主数据）与自定义分类

**入口**：首页「仓库管理」→ 品类列表 / 新增品类；「分类管理」维护自定义分类。

**功能点：**

1. 新增啤酒品类：填写名称、分类（从自定义分类中选择）、规格（如 500ml×12瓶/箱）、售价、成本价、保质期（天数）。
2. 编辑品类：修改售价、成本价、保质期、分类。
3. 停用/启用品类（软删除，保留历史流水关联）。
4. 查询：按名称/分类搜索。

**自定义分类：**

- 分类由用户**自定义维护**，可新增、重命名、停用分类；被品类引用的分类不可删除，仅可停用。
- 库存统计、销售排行等均按自定义分类分组。

**字段：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | string | 是 | 啤酒名称 |
| categoryId | string | 是 | 分类 id（引用自定义分类） |
| spec | string | 否 | 规格/容量描述（如 500ml） |
| sellingPrice | number | 是 | 售价（元） |
| costPrice | number | 是 | 成本价（元，用于利润/损失核算） |
| shelfLifeDays | number | 是 | 保质期天数（用于推算到期日） |
| stock | number | 是 | 当前总库存（只读，由批次库存汇总，事务维护） |
| status | enum | 是 | active / disabled |
| createdAt / updatedAt | date | 是 | 创建/更新时间 |

> 计量单位统一为 **「瓶」**，所有数量、统计口径均以「瓶」为单位，不做瓶/箱换算。保质期采用「天数」存储，入库时由「生产日期 + shelfLifeDays」计算到期日；生产日期可缺省（见 §3.2）。

---

### 3.2 进货入库（增加库存）

**入口**：首页「进货入库」。

**功能点：**

1. 选择啤酒品类。
2. 填写入库数量（瓶）、进货单价（默认取品类成本价）、生产日期（**可选**）、订单号/备注。
3. 提交后（同一事务内）：新建一条「批次库存」、品类 `stock += 数量`、写入一条「入库流水」。
4. 入库成功后提示，并可选跳转查看库存。

**生产日期与到期日规则：**

- **生产日期非必填**。已填 → 到期日 = 生产日期 + shelfLifeDays，参与临期/过期提醒。
- **未填 → 批次无到期日**，不参与临期/过期判断，但在批次列表、出库选批次、库存明细处显示 **红色「未填」标识**，提示管理员及时补录。

**校验：**
- 数量必须为正整数；
- 进货单价需 > 0。

**入库流水字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| type | enum | `in` |
| beerId | string | 品类 id |
| batchId | string | 新建批次 id |
| quantity | number | 入库数量（瓶） |
| unitPrice | number | 进货单价 |
| productionDate | date \| null | 生产日期（可选） |
| expiryDate | date \| null | 到期日（已填生产日期时推算，否则 null） |
| orderNo | string | 订单号（可选） |
| operatorId | string | 操作人 openid |
| remark | string | 备注 |
| createdAt | date | 时间 |

---

### 3.3 销售出库（减少库存，批次组合出库）

**入口**：首页「销售出库」。

**功能点：**

1. 选择啤酒品类。
2. 系统列出该品类所有「在库批次」（生产日期/未填标识、到期日、剩余数量）。
3. **批次组合出库**：管理员可勾选多个不同生产日期的批次，分别填写每个批次的出库数量；也可点「按到期日自动分配（先到期先出）」一键按 FEFO 预填后再手动调整。
4. 填写销售单价（默认取品类售价）、订单号/备注。
5. 提交校验：各批次出库数量之和 = 总出库数量，且每个批次出库量 ≤ 该批次剩余数量，否则拦截提示「库存不足」。
6. 同一事务内：按所选批次扣减各批次 `quantity`、品类 `stock -= 总出库数量`、写入「出库流水」（一次出库涉及多批次时按批次拆分多条流水，用同一 `orderNo` 关联）。
7. 出库成功后提示。

**出库流水字段（在入库字段基础上增加销售核算字段）：**

| 字段 | 类型 | 说明 |
|------|------|------|
| type | enum | `out` |
| beerId | string | 品类 id |
| batchId | string | 扣减的批次 id |
| quantity | number | 本批次出库数量（瓶） |
| unitPrice | number | 销售单价 |
| costPrice | number | 成本快照 = 品类 costPrice（简化口径，见 §11） |
| amount | number | 销售额 = quantity × unitPrice |
| profit | number | 毛利 = amount − quantity × costPrice |
| orderNo | string | 订单号（一次出库多批次共用） |
| operatorId | string | 操作人 openid |
| remark | string | 备注 |
| createdAt | date | 销售时间 |

---

### 3.4 库存统计界面（仓库总量 + 分品类数量）

**入口**：首页「库存统计」。

**展示内容：**

1. **顶部总览卡片**：仓库啤酒总库存（所有品类 stock 之和，单位「瓶」）。
2. **分类汇总**：按自定义分类（categoryId）分组统计各分类数量。
3. **品类明细列表**：每个品类的名称、分类、当前库存、售价、最早到期日、状态标识（临期/过期高亮、未填生产日期红点）。
4. **筛选**：按分类筛选、按状态筛选（全部/临期/已过期/未填生产日期）。

**关键指标定义：**

- 总库存 = Σ(品类.stock)；
- 临期 = 存在在库批次（quantity>0 且 expiryDate 非空）到期日在「今天 + N 天」内（N 默认 **45**，可在设置中修改）；
- 已过期 = 存在在库批次（quantity>0 且 expiryDate 非空）到期日 < 今天；
- 未填生产日期 = 存在在库批次（quantity>0）productionDate 为空。

---

### 3.5 仓库管理界面（剩余库存 / 售价 / 最低保质期时长）

**入口**：首页「仓库管理」。

**品类列表每条展示（重点字段）：**

| 字段 | 说明 |
|------|------|
| 名称 / 分类 | 啤酒名称与所属自定义分类 |
| 剩余库存 | 该品类当前总库存（stock，单位瓶） |
| 售价 | sellingPrice（元） |
| 最低保质期时长 | 该品类「在库批次（quantity>0 且 expiryDate 非空）」中最早到期批次的剩余天数（见下） |

**「最低保质期时长」定义：**

- 计算方式：`min(批次.expiryDate) − 今天`，其中批次为「该品类、quantity > 0、expiryDate 非空」的在库批次。
- 展示规则：
  - `> N 天` → 正常（灰色/绿色）；
  - `1 ~ N 天` → 临期（橙黄高亮）；
  - `≤ 0 天` → 已过期（红色高亮）；
  - 无在库批次 → 显示「—」；
  - 全部批次未填生产日期 → 显示「未填生产日期」（红色标识）。

**编辑功能点：**

1. 编辑单个品类：修改 **售价**、**成本价**、**保质期天数**、分类。
2. 修改即时生效，保存后更新 `updatedAt`。
3. 支持新增品类、停用品类、维护自定义分类。

> 说明：售价/成本/保质期属于「主数据」，修改影响后续出入库默认值，不追溯历史流水。

---

### 3.6 过期提醒（微信订阅消息）

这是本项目体验的关键功能，单独说明机制。

**需求：** 当某批次啤酒「即将过期」或「已经过期」时，小程序向所有管理员发送微信消息提醒。

**微信订阅消息机制（重要约束）：**

微信「订阅消息」替换了旧的模板消息，核心规则：

1. **必须用户主动订阅**：发送前必须由用户在小程序内点击授权 `wx.requestSubscribeMessage({ tmplIds })`。
2. **一次性订阅消息**：每授权一次，只能发送 **1 条** 消息；发送后授权即消耗。用户授权一次 = 可获得 1 条提醒。
3. **长期订阅消息**：无需重复订阅、可长期发送，但仅对少数资质类别开放（医疗、政务、公共服务等），普通商贸类小程序通常**无法申请长期订阅**，只能使用「一次性订阅消息」。

**因此本期方案采用「一次性订阅消息」+ 订阅授权：**

- 在「库存统计」「仓库管理」等关键页面，引导管理员点击「开启临期/过期提醒」，调用 `wx.requestSubscribeMessage` 一次性申请多条订阅授权（`tmplIds` 为临期提醒、过期提醒两个模板）。
- 后端云函数用**定时触发器（定时器）**每日执行，扫描在库批次：
  - 到期日 - 今天 ≤ N 天 且 quantity > 0 且 expiryDate 非空 → 视为「临期」；
  - 到期日 < 今天 且 quantity > 0 且 expiryDate 非空 → 视为「已过期」。
  - 生产日期未填的批次**不参与**临期/过期提醒，但通过「未填」红点引导补录。
- 对满足条件的批次，向**每位已订阅的管理员**使用其累积授权，通过 `cloud.openapi.subscribeMessage.send` 逐条发送提醒。
- 由于「一次性订阅」授权会消耗，**页面应常驻「续订提醒」引导**，提示用户每次授权对应可收到的提醒次数，尽量让授权条数 > 预期提醒次数。

**消息模板字段（示意，最终以微信公众平台审核通过的模板为准）：**

| 模板 | 字段 |
|------|------|
| 临期提醒 | 商品名称 `thing`、到期日期 `date`、剩余数量 `number`、提醒内容 `thing` |
| 过期提醒 | 商品名称 `thing`、到期日期 `date`、剩余数量 `number`、提醒内容 `thing` |
| 新订单提醒 | 订单号 `character_string`、商品 `thing`、金额 `amount`、下单时间 `time` |

> **关于「模板资质」**：微信订阅消息使用的模板必须是「微信公众平台 → 订阅消息」中**属于你的小程序、且已通过审核的类目模板**，不能自定义字段。你需要在小程序的服务类目下，从微信提供的模板库中选取与「库存/商贸/生活服务」相关的模板（如「库存变动提醒」「商品临期提醒」等），拿到模板 ID（templateId）后才能发送。字段类型必须与模板定义一致（`thing` 限 20 字符、`date` 限时间格式、`number` 限数字）。若类目下无合适模板，需先调整服务类目或更换语义接近、字段可对上的模板。

**降级方案（订阅授权用尽或未授权时）：**
- 小程序内首页「待处理提醒」红点/列表兜底展示临期与过期清单，保证即使微信消息发不出，用户打开小程序也能看到。

**新订单提醒（客人下单事件触发）：**
- 客人通过商城 `createOrder` 下单成功后，向已订阅「新订单提醒」的管理员发送订阅消息（订单号 / 商品 / 金额 / 下单时间），便于及时处理待处理订单。
- 与临期/过期不同，新订单提醒为**事件触发**（下单事务提交后发送，失败静默、不阻塞下单），无需定时触发器。

---

### 3.7 销售统计（当日 / 当月 / 当年）

**入口**：首页「销售统计」。

**展示内容：**

1. **顶部切换 Tab**：当日 / 当月 / 当年。
2. **金额总览卡片**：所选周期内 **销售额合计**、**净利润（毛利口径）合计**、订单笔数、销量合计。
3. **数据分析趋势图**：展示 **销售额趋势** 与 **净利润趋势**（折线/柱状），时间粒度可选 **周 / 月 / 季度 / 年**。
4. **品类销量/销售额排行**：按销售额、销量、毛利排序的品类 Top N。

**统计口径：**

- 当日：`createdAt` 在 [今日 00:00, 明日 00:00)；
- 当月：`createdAt` 在 [当月 1 日 00:00, 次月 1 日 00:00)；
- 当年：`createdAt` 在 [当年 1 月 1 日, 次年 1 月 1 日)。
- 销售额 = Σ(`type ∈ {out, return}`.amount)；净利润 = Σ(`type ∈ {out, return}`.profit)。
- 退货以负数冲减销售额与利润；报废、盘亏单独计入「损失」，不计入销售额（见 §3.9~§3.11）。

**实现方式**：云函数按时间范围对 `transactions` 中 `type ∈ {out, return}` 记录做聚合（`aggregate().group().sum()`），返回汇总结果与按日/月分组序列。

---

### 3.8 出入库流水界面（销售额 / 净利润 / 趋势图）

**入口**：首页「流水」。

**展示内容：**

1. **顶部汇总卡片**：所选时间范围内 **销售额**、**净利润（毛利口径）**、出入库笔数、报废/盘亏损失。
2. **数据分析趋势图**：展示销售额与净利润趋势（折线/柱状，可切换「销售额 / 净利润」指标），时间粒度可选 **周 / 月 / 季度 / 年**，并支持自定义时间范围。
3. **流水明细列表**（时间倒序）：每条显示类型（进货/出库/退货/报废/盘点）、品类名称、数量、单价、金额；出库/退货记录额外显示毛利。支持分页加载。
4. **筛选**：类型（全部/进货/出库/退货/报废/盘点）、品类、操作人、时间范围（近 7 天/近 30 天/本月/自定义）。

**指标定义（与销售统计一致）：**

- 销售额 = Σ(`type ∈ {out, return}`.amount)；
- 净利润（毛利口径）= Σ(`type ∈ {out, return}`.profit) = 销售额 − 销售成本（已售商品成本，按品类成本价），**未扣除房租、人工等经营费用**（见 §11）。

---

### 3.9 退货入库

**入口**：首页「退货入库」。

**功能点：**

1. 选择啤酒品类，填写退货数量、退货单价（默认取品类售价）、退回批次的生产日期/到期日（可选）、退货原因。
2. 提交后（同一事务内）：新建或恢复到对应批次、品类 `stock += 退货数量`、写入一条 `return` 流水。
3. 退货流水 `amount`、`profit` 记**负数**，用于冲减销售额与利润。

**退货成本冲回口径（已确认）：**
- 退货 `amount`（负）= − 退货数量 × 退货单价；
- 退货 `profit`（负）= − 退货数量 ×（退货单价 − 品类成本价），即成本按品类成本价冲回。

---

### 3.10 报废出库

**入口**：首页「报废出库」。

**功能点：**

1. 选择品类 + 批次（默认推荐最早到期批次，可手动选择）、报废数量、报废原因（临期/损坏/其他）。
2. 提交后（同一事务内）：扣减批次 `quantity`、品类 `stock -= 报废数量`、写入一条 `scrap` 流水。
3. 报废损失 = 报废数量 × 品类成本价，单独展示，**不计入销售额**。

**报废流水字段：** `type = scrap`，`quantity` 报废数量，`costPrice` 成本，`amount`（损失）= quantity × costPrice，`remark` 记录原因。

---

### 3.11 库存盘点（支持局部盘点）

**入口**：首页「库存盘点」。

**功能点：**

1. 创建盘点单：**支持全量盘点，也支持按品类局部盘点**（只勾选部分品类录入实际数量，其余品类保持不变）。
2. 逐品类录入实际数量（支持分批保存草稿）。
3. 系统对比账面 `stock` 与实际数量，计算 **盘盈**（实际 > 账面）/**盘亏**（实际 < 账面）。
4. 提交后（同一事务内）：将参与盘点的品类 `stock` 调整为实际数量，写入 `adjust` 流水（盘盈为正、盘亏为负）。
5. 盘亏金额 = 盘亏数量 × 品类成本价，计入「损失」。
6. 盘点历史列表：查看历次盘点单与差异明细。

**盘点流水字段：** `type = adjust`，`quantity` 调整量（正=盘盈，负=盘亏），`remark` 记录盘点单号与说明。

---

### 3.12 数据导出（Excel / CSV）

**入口**：首页「数据导出」，或各统计/流水页的「导出」按钮。

**功能点：**

1. 导出对象：**流水明细**、**销售统计**、**库存清单**、**盘点记录**。
2. 导出格式：**CSV**（逗号分隔，含 BOM 处理 Excel 中文乱码）；流水/盘点可按时间范围筛选，销售统计可选周期。
3. 实现方式：云函数 `exportData` 内存生成 CSV 文本直接返回，前端 `wx.setClipboardData` 复制到剪贴板，粘贴到 Excel/记事本保存（无第三方依赖）。

> 说明：本期采用 CSV 剪贴板方案（最稳定、免依赖）；`.xlsx` 文件下载（node-xlsx + 云存储 + `openDocument`）留作后续增强。

---

### 3.13 多管理员与权限

**目标：** 允许多个管理员访问同一仓库，进行出入库操作与查看数据，操作留痕。

**成员模型：**

- 新增 `admins` 集合存储授权成员：`{ openid, nickname, role, status, createdAt }`。
- **所有者（owner）**：首位使用者自动成为 owner；拥有全部功能，且**独有「成员管理」**（添加/移除管理员、生成邀请码、移交 owner）；支持多 owner（owner 可将 operator 提升为 owner，原 owner 保持）。
- **操作员（operator）**：被邀请加入；除「成员管理」外拥有全部业务权限——出入库、退货/报废/盘点、查看数据与统计、导出、新增/修改/**删除**品类与分类（含售价/成本/保质期）；**仅不可管理成员**。

**加入机制：**

- owner 在「成员管理」页生成**邀请码**，新管理员首次进入小程序输入邀请码 → 后端记录其 openid 为 operator。
- 或由 owner 直接录入对方 openid（需对方先登录一次以获取 openid）。

**操作留痕：**

- 所有流水（进出货/退货/报废/盘点）均记录 `operatorId`，流水界面可按「操作人」筛选，追溯责任。

### 3.14 消费者商城（展示销售）

**入口**：小程序底部 tabBar「商城 / 购物车 / 我的」。

**角色分流**：`getMyRole` 根据 openid 判断身份——首位打开者自动成为 owner，其余非白名单用户为 customer；管理员与客人打开同一小程序看到不同界面。

**功能点：**

1. **商品展示**：商城页展示在售（`status=active` 且 `stock>0`）啤酒品类，含名称、规格、售价、库存、分类筛选；商品卡带啤酒图标（`icon` 字段，缺省 🍺）。
2. **加入购物车**：点击「＋」将品类加入购物车（本地缓存），可增减数量、删除。
3. **下单结算**：填联系人姓名 + 电话 + 备注 → 提交生成订单。**不做线上支付**，线下交付。
4. **下单即扣库存**：生成订单时事务内按 FEFO 扣减批次与库存、写 `out` 流水（计入销售额/净利润），订单状态 `pending`。
5. **订单状态**：管理员在「订单管理」可将待处理订单「完成」或「取消」；取消时回补库存并写 `return` 负流水冲减销售。
6. **我的订单**：客人「我的」页查看自己的订单与状态。

**数据安全**：商城只下发展示字段（不下发 `costPrice`/`shelfLifeDays`）；管理侧接口均加 openid 白名单校验，客人无法调用入库/出库/统计等。

---

## 4. 前端设计规范（UI/UX）

> 设计原则：**简洁、大方、易操作**。信息层级清晰，关键操作一键可达，尽量减少输入负担。

### 4.1 视觉风格

- **扁平化 + 卡片式**：大卡片承载数据模块，卡片圆角统一（8~12rpx），背景浅灰、卡片白色，留白充足。
- **信息层级**：标题（粗/深色）→ 数值（大字号、等宽数字）→ 辅助说明（小字号、灰色）。
- **颜色统一**（建议配色）：

| 用途 | 色值 | 说明 |
|------|------|------|
| 品牌主色 | `#F5A623` 琥珀金 | 呼应啤酒主题，用于关键按钮、选中态、高亮 |
| 标题文字 | `#1F2937` 深灰 | 主标题、关键数值 |
| 次要文字 | `#6B7280` 中灰 | 说明、标签 |
| 背景/分隔 | `#F3F4F6` 浅灰 | 页面背景、分隔线 |
| 正常 | `#16A34A` 绿 | 库存正常、净利润为正 |
| 临期 | `#F59E0B` 橙黄 | 临期高亮 |
| 过期/告警 | `#EF4444` 红 | 已过期、库存不足、未填生产日期、净利润为负 |

### 4.2 布局与交互

- **首页**：宫格大图标菜单（进货入库 / 销售出库 / 库存统计 / 仓库管理 / 销售统计 / 流水），每项配图标 + 文字，点按区域 ≥ 88px，符合微信可点触规范；次要入口（退货/报废/盘点/导出/成员/分类）收纳在「更多」或独立宫格。
- **数据卡片**：核心数字（总库存、销售额、净利润）以大字号 + 单位呈现，一眼可读。
- **表单**：能默认就默认（出库单价默认售价、入库单价默认成本价），能选就不填（品类/分类用选择器），减少输入。
- **批次选择器**：出库/报废时以列表展示各批次（生产日期/到期日/剩余数量），支持勾选与逐项填数量，未填生产日期批次显示红点。
- **防误操作**：出库、报废、盘点提交前二次确认；停用品类、移除成员等破坏性操作需确认。
- **操作反馈**：成功/失败用 `wx.showToast` 提示；库存不足等错误用醒目提示并给出原因。
- **趋势图**：折线/柱状展示销售额与净利润，主色表示销售额、辅助色表示净利润，图例清晰；点击数据点可查看具体数值。

### 4.3 数据可视化约定

- 趋势图统一使用 **echarts-for-weixin**（ECharts 小程序版）或 canvas 自绘；微信小程序无内置图表组件，需在技术方案中确认引入方式。
- 销售额与净利润两条序列配色区分明确（如销售额用品牌主色、净利润用蓝色），避免仅靠颜色区分导致可读性问题。

---

## 5. 页面结构

| 页面路径 | 名称 | 说明 |
|----------|------|------|
| `pages/index/index` | 首页 | 功能宫格菜单（进货/出库/库存/仓库管理/销售统计/流水 + 更多），显示待处理提醒红点 |
| `pages/inbound/index` | 进货入库 | 增加库存 |
| `pages/outbound/index` | 销售出库 | 批次组合减少库存 |
| `pages/return/index` | 退货入库 | 冲减销售额、恢复库存 |
| `pages/scrap/index` | 报废出库 | 移除临期/损坏库存 |
| `pages/stocktake/index` | 库存盘点 | 账实核对、盘盈盘亏调整（支持局部） |
| `pages/stock/index` | 库存统计 | 总量 + 分品类 + 临期/过期/未填标识 |
| `pages/manage/index` | 仓库管理 | 品类列表（剩余库存/售价/最低保质期）+ 编辑 + 分类管理 |
| `pages/statistics/index` | 销售统计 | 当日/当月/当年销售额与净利润 + 趋势图 + 排行 |
| `pages/ledger/index` | 流水 | 出入库流水 + 销售额/净利润 + 趋势图 |
| `pages/members/index` | 成员管理 | 邀请/移除管理员、角色设置（owner） |
| `pages/export/index` | 数据导出 | 导出流水/统计/库存/盘点 |
| `pages/shop/index` | 商城 | 商品展示 + 分类筛选 + 加购 |
| `pages/cart/index` | 购物车 | 购物车 + 结算下单 |
| `pages/mine/index` | 我的 | 角色识别 + 我的订单 + 管理后台入口 |
| `pages/orders/index` | 订单管理 | 管理员查看/处理客人订单 |

> 首页沿用现有 `powerList` 声明式菜单的交互模式，各功能项跳转到对应页面。

---

## 6. 数据模型（CloudBase 文档数据库集合）

### 6.1 `beers` — 啤酒品类（SKU 主数据）

```
{
  _id, name, categoryId, spec,
  icon,             // 商城商品图标（emoji，可选，缺省 🍺）
  image,            // 商品实拍图（cloud:// 文件 ID，可选，优先于 icon 显示）
  sellingPrice, costPrice, shelfLifeDays,
  stock,            // 当前总库存（瓶，denormalized，事务维护 = Σ batches.quantity）
  status,           // active / disabled
  createdAt, updatedAt
}
```

### 6.2 `categories` — 自定义分类

```
{
  _id, name,         // 分类名称（用户自定义）
  sort, status,      // status: active / disabled
  createdAt
}
```

### 6.3 `batches` — 批次库存（临期/过期/最低保质期的数据源）

```
{
  _id, beerId,
  quantity,          // 该批次剩余数量（瓶）
  productionDate,    // 可空；为空则该批次无到期日，标记「未填」
  expiryDate,        // 可空；= productionDate + shelfLifeDays
  inboundTxnId, createdAt
}
```

- 入库新建批次；出库按管理员所选批次扣减；报废扣减对应批次。
- 临期/过期判断、最低保质期时长均基于 `quantity > 0` 且 `expiryDate` 非空的批次。

### 6.4 `transactions` — 出入库流水（审计 + 统计）

```
{
  _id, type,         // in / out / return / scrap / adjust
  beerId, batchId, quantity, unitPrice,
  productionDate, expiryDate,   // 入库/退货时使用
  costPrice, amount, profit,    // 出库/退货/报废使用（成本快照=品类 costPrice）
  orderNo, operatorId, remark, createdAt
}
```

| type | 含义 | 库存 | amount / profit |
|------|------|------|------|
| `in` | 进货入库 | + | — |
| `out` | 销售出库 | − | amount=销售额，profit=毛利 |
| `return` | 退货入库 | + | amount/profit 为负，冲减销售 |
| `scrap` | 报废出库 | − | amount=报废损失 |
| `adjust` | 盘点调整 | ± | 盘亏记损失（可选） |

### 6.5 `admins` — 管理员成员

```
{
  _id, openid, nickname, role,   // role: owner / operator
  status, createdAt
}
```

### 6.6 `orders` — 客人订单

```
{
  _id, orderNo, customerOpenid, customerName, customerPhone,
  items: [{ beerId, name, spec, unitPrice, quantity, amount }],  // 下单时快照
  totalQuantity, totalAmount, status,   // pending / completed / cancelled
  remark, createdAt
}
```

### 6.7 `invites` — 邀请码

```
{
  _id, code, createdBy, createdAt,
  expiresAt,           // 7 天有效期
  status,              // active / used / cancelled
  usedBy, usedAt
}
```

### 6.8 `subscriptions` — 订阅消息额度

```
{
  _id, openid,               // 管理员 openid
  expiringQuota,             // 临期提醒剩余可发条数
  expiredQuota,              // 过期提醒剩余可发条数
  newOrderQuota,             // 新订单提醒剩余可发条数
  totalGranted, totalSent,   // 累计授权 / 累计已发
  updatedAt
}
```

- 一次性订阅「每条授权=1 条」；`subscribeRemind` 累加额度，发送成功后相应 quota 减 1。

### 6.9 `settings` — 全局设置

```
{
  _id: "global",        // 固定单文档
  expiringDays,         // 临期阈值（天），默认 45
  updatedAt
}
```

- 仅一份；`updateSettings` 用 `doc("global").set` 写入，读取走 `getExpiringDays()`（未配置回退默认 45）。

### 6.10 索引建议

- `beers`：`categoryId` 索引、`status` 索引。
- `batches`：`beerId` + `expiryDate` 复合索引（供过期扫描与最低保质期计算）；`expiryDate` 索引。
- `transactions`：`type` + `createdAt` 复合索引（供销售统计/趋势）；`beerId` + `createdAt`、`operatorId` + `createdAt` 复合索引（供流水筛选）。
- `admins`：`openid` 唯一索引。
- `orders`：`customerOpenid` 索引、`status` + `createdAt` 复合索引（供订单筛选）。
- `invites`：`code` 索引（供邀请码校验）。
- `subscriptions`：`openid` 唯一索引（供订阅额度查询）。
- `settings`：单文档，无需额外索引。

---

## 7. 后端云函数设计

沿用现有单云函数 `quickstartFunctions` 的 `switch(event.type)` 分发模式，新增以下 `type` 处理函数：

| type | 说明 |
|------|------|
| `getBeers` | 查询品类列表（含 stock、售价、最低保质期时长、分类名，供仓库管理界面） |
| `addBeer` | 新增品类 |
| `updateBeer` | 编辑品类（售价/成本/保质期/分类/停用） |
| `getCategories` / `addCategory` / `updateCategory` | 自定义分类维护 |
| `inbound` | 进货入库（新建批次 + 增库存 + 写流水） |
| `outbound` | 销售出库（校验库存 + 按所选批次扣减 + 减库存 + 写流水，含 amount/profit） |
| `returnGoods` | 退货入库（增库存 + 写负金额流水） |
| `scrap` | 报废出库（扣批次 + 减库存 + 写损失流水） |
| `stocktake` | 盘点（支持局部；对比账面/实际，调整库存，写 adjust 流水） |
| `getStockStats` | 库存统计（总量、分类汇总、明细、临期/过期/未填标识、最低保质期时长） |
| `getSalesStats` | 销售统计（销售额、净利润、笔数、销量、趋势、排行） |
| `getTransactions` | 流水查询（分页、筛选、汇总销售额/净利润） |
| `exportData` | 生成 CSV 文本（含 BOM），前端复制剪贴板（流水/库存/销售/盘点四类） |
| `getAdmins` / `addAdmin` / `removeAdmin` / `transferOwner` / `createInvite` | 成员管理（仅 owner）：列表 / 录入 openid / 软删除移除 / 移交 owner（多 owner）/ 生成邀请码 |
| `joinByInvite` | 邀请码加入（公开：customer 输码成为 operator） |
| `subscribeRemind` | 记录订阅授权（仅 admin）：累加 subscriptions 中临期/过期/新订单三类可发额度 |
| `getSettings` / `updateSettings` | 全局设置：读/写临期阈值 N（`settings/global`；仅 admin 可写） |
| `getRemindQuota` | 当前管理员订阅消息剩余额度（续订引导，仅 admin） |
| `getRemindList` | 待处理提醒兜底列表：临期/过期批次清单（首页红点，仅 admin） |
| `getMyRole` | 角色判断（首位打开者自动成为 owner） |
| `getShopBeers` | 商城商品列表（公开，仅展示字段） |
| `createOrder` | 客人下单（事务：FEFO 扣批次 + 减库存 + 写 out 流水 + 建订单） |
| `getOrders` | 订单查询（管理员全量/客人自己） |
| `updateOrderStatus` | 订单状态流转（完成/取消，取消回补库存） |

**关键实现要求：**

1. **出入库/退货/报废/盘点库存一致性**：所有库存变更使用数据库事务（`db.runTransaction`）保证「更新 stock」「新建/扣减批次」「写流水」原子性，防止并发下库存错乱。出库需在事务内逐批次校验剩余数量。
2. **权限校验**：写操作在云函数内校验调用者 openid 是否为 `admins` 中有效成员；**仅「成员管理」为 owner 专属**；其余写操作（出入库/退货/报废/盘点、新增/修改/删除品类与分类）owner 与 operator 均可。所有流水记录 `operatorId`。
3. **定时触发器**：新增定时云函数（或为 `quickstartFunctions` 配置定时触发器），每日执行，扫描 `batches` 中 `quantity > 0` 且 `expiryDate` 非空且临期/过期的批次，向已订阅管理员发送订阅消息。
4. **订阅消息发送**：使用 `cloud.openapi.subscribeMessage.send`，`touser` 为各管理员 openid，`templateId` 与 `page`（点击跳转库存统计页）需在云函数 `config.json` 中声明 `subscribeMessage.send` 权限。
5. **导出依赖**：`exportData` 需引入 xlsx 生成库，部署时选择「云端安装依赖」。

---

## 8. 技术要点与风险

| 事项 | 说明 |
|------|------|
| 云环境 env | 部署前必须配置 `app.js` 中 `globalData.env` 为正确的云环境 ID |
| 订阅消息一次性限制 | 每条授权仅发 1 条；需常驻续订引导，并做好小程序内兜底提醒 |
| 订阅消息模板资质 | 需在微信公众平台申请并通过类目审核，字段类型须与模板一致 |
| 定时触发器频率 | 云函数定时触发器按 cron 配置，注意避免高频（每日 1 次即可） |
| 到期日口径 | 临期阈值 N 默认 45 天，可在设置中修改 |
| 并发与库存 | 出入库必须事务化 + 批次扣减，禁止「先查后改」的非原子写 |
| 趋势图实现 | 微信小程序无内置图表，需引入 echarts-for-weixin 或 canvas 自绘 |
| 数据导出 | 云函数需引入 xlsx 依赖，注意云端安装依赖与文件大小限制 |
| 多管理员并发 | 多管理员同时操作同一品类需靠事务保证库存一致；操作留痕便于排查 |
| 邀请码安全 | 邀请码需限时/可作废，防止他人冒用加入 |

---

## 9. 非功能需求

- **性能**：库存统计、销售统计、流水查询在千级品类/万级流水规模下，查询响应 < 1s（依赖索引）；流水列表分页加载。
- **安全**：所有库存/销售写操作均在云函数内完成，前端不直接写数据库；以 `cloud.getWXContext().openid` 识别身份，并校验成员权限；仅「成员管理」为 owner 专属。
- **可用性**：订阅消息发不出时，小程序内「待处理提醒」列表兜底，不影响核心进销存功能。
- **数据一致性**：出入库/退货/报废/盘点事务化 + 批次扣减，保证 stock、批次库存、流水三者一致。

---

## 10. 里程碑（建议）

| 阶段 | 内容 | 产出 |
|------|------|------|
| M1 | 品类管理 + 自定义分类 + 进货入库 + 销售出库（批次组合） | 可用的进销存闭环 |
| M2 | 库存统计 + 仓库管理（剩余库存/售价/最低保质期）+ 销售统计（含净利润） | 核心统计与维护界面 |
| M3 | 流水界面 + 退货/报废/盘点（局部） | 经营分析与库存调整 |
| M4 | 消费者商城 + 权限（openid 白名单） | 商城下单闭环 + 管理员权限拦截 |
| M5 | 数据导出 + 多管理员协作（邀请码/成员管理） | 协作与导出 |
| M6 | 临期/过期/新订单订阅消息提醒 + 定时触发器 | 微信消息提醒 |
| M7 | 打磨：续订引导、兜底提醒、未填生产日期红点、阈值配置、图表优化、商品实拍图 | 上线版本 |

---

## 11. 已确认决策与待办依赖

**已确认决策汇总：**

- 净利润按毛利口径 = 销售额 − 销售成本（成本取品类成本价快照）。
- 生产日期非必填，未填批次显示红点标识。
- 计量单位统一「瓶」。
- 库存盘点支持按品类局部盘点。
- 啤酒分类支持自定义。
- 多管理员两角色：owner（全部 + 独有成员管理）、operator（除成员管理外全部业务权限，**可修改和删除品类/分类**）。
- 退货成本按品类成本价冲回。
- 临期提醒阈值 **N 默认 45 天，可在设置中修改**。
- 数据导出：导出前可选择 Excel 或 CSV。
- 趋势图时间粒度：可选 **周 / 月 / 季度 / 年**。
- 不设「只读」角色。

**待办依赖（非功能决策，属落地动作）：**

1. **订阅消息模板**：需在微信公众平台申请/选择与「库存/商贸」类目匹配、已通过审核的订阅消息模板（临期、过期各一），获取 templateId。此为上线前的必要依赖，详见 §3.6。
